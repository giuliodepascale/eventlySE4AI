/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://eventlyitalia.com', // Cambia con il dominio del tuo sito
    generateRobotsTxt: true, // Genera automaticamente un file robots.txt
    sitemapSize: 5000, // Numero massimo di URL per sitemap (opzionale)
  };
  
  module.exports = config;
  